

### 1. 本周完成的工作



- 实验设计与实施：
- 数据收集与处理：
- 算法开发与优化：
- 文献阅读：
  
  - 完成AeroGen改进的第一步：构建base detector，获得所需的 `Positive RoI features` 和 `Class-ambiguous features`，具体实现如下：
  
  - ###   构建基础检测器（Base Detector）
  
    ###   目标：从输入图像中提取 `Positive RoI features` 和 `Class-ambiguous features`，用于后续的双流关系学习。
  
       输入图像
  
    ​           ↓
  
       冻结：Backbone: Feature Extraction
  
    ​           ↓
  
       冻结：FPN: Multi-level Feature Aggregation
  
    ​           ↓
  
       冻结：RPN: Region Proposal Network（生成候选框 + 分数）
  
    ​           ↓
  
       冻结：Rotated RoIAlign（用于 RS 场景，保留角度信息）
  
    ​           ↓
  
       冻结：Proposal 分类（按置信度划分为 Positive / Ambiguous / Negative）
  
    ​           ↓
  
       输出：{ri,j}nr + {ci,j}nc
  
      用到的 base detector 是 Faster-RCNN + RoIAlign。
  
    1. 用一个检测模型（如 pretrained Faster-RCNN）对 layout/mask-control 指定区域进行目标 proposal；
    2. 输出三类 proposal：
       1. Positive proposals（高置信度）
       2. Ambiguous proposals（中等置信度）
       3. Negative proposals（低置信度，不用）
    3. 代码实现逻辑：
  
      `使用现有 layout/mask 生成候选框 proposals` `proposals = generate_region_proposals(mask_control) ` `# 分类 RoI proposals（置信度由 pretrained 分类器给出）` `positive_rois, ambiguous_rois = split_proposals_by_score(proposals, threshold_pos=0.7, threshold_neg=0.3) ` `# 提取 RoI feature：正样本和模糊样本` `roi_features = roi_align(x_feature_map, positive_rois + ambiguous_rois)`
- 论文撰写：

### 2. 遇到的困难和问题



- 技术难点：
- 理论疑惑：
- 实验问题：
- 其他问题

### 3. 解决方案与已采取的措施



- 问题分析：
- 解决方法：
- 寻求的帮助：

### 4. 下周工作计划



- 主要目标：
  - 完成后续的DCL模块的构建
- 具体任务：
- 时间安排：

### **5. 需要的支持与资源**



- 导师建议：
- 资源需求：

### 6. 其他备注



- 个人心得：
  - 继续努力！