### 本学期工作总结

本学习继续深入工业异常检测领域，不过从单一类型异常检测逐渐转移到零样本工业异常检测，其中以基于CLIP的零样本工业异常检测为主。

#### 单一类型异常检测

有内存库、S-T架构、重建、生成、嵌入等方法，部分论文如下：

- AnoDDPM: Anomaly Detection with Denoising DiffusionProbabilistic Models using Simplex Noise, 扩散模型首次做工业检测任务
- High-Resolution Image Synthesis with Latent Diffusion Models，LDM潜在空间扩散模型
- Taming Transformers for High-Resolution Image Synthesis，看这篇是因为LDM的编码器在这篇文章
- ADer: A Comprehensive Benchmark for Multi-class Visual Anomaly Detection，多类视觉异常检测的综合基准
- DRÆM – A discriminatively trained reconstruction embedding for surface anomaly detection， DREAM合成异常方法是亮点
- 工业缺陷检测无监督深度学习方法综述，知网找的论文
- 基于深度学习的工业产品图像异常检测算法研究， 知网硕士毕业论文（电子科技大学），主要看写作以及内容
- 基于语义分割的钢材缺陷检测算法研究，知网硕士毕业论文（浙江大学），专注于目标检测了而非工业异常，同样主要看写作和内容
- Diversity-Measurable Anomaly Detection，这篇是异常检测排行榜的第二，但是方法和别的不太一样，看不懂。
- ReConPatch : Contrastive Patch Representation Learning for Industrial Anomaly Detection，同样排行榜前几，没认真细读
- Deep Industrial Image Anomaly Detection: A Survey，最新的综述，很全面
- RealNet: A Feature Selection Network with Realistic Synthetic Anomaly for Anomaly Detection，其中的异常生成模块收集了一个数据集，并且论文比较新，效果也好，方法是重建的方法
- Unsupervised Two-Stage Anomaly Detection，两阶段重建方法
- A Survey on RGB, 3D, and Multimodal Approaches for Unsupervised Industrial Anomaly Detection，里面有零样本异常检测调研

#### 单一类型异常检测心得

- 基于嵌入的任务依赖于预训练模型提取特征，学习正常样本的分布，从而可以检测出异常样本的不正常分布。嵌入任务中的知识蒸馏，教师网络使用预训练模型训练，学生网络则不使用，在训练阶段学习正常样本的分布，测试阶段由于异常样本和正常样本差异较大，学生网络偏离较大，而教师网络由于有预训练模型，故而偏差相对较小。主要调整的是两个网络的架构以及损失函数的约束。One-class classification这种方法是用正常样本训练一个边界，使得正常样本的特征都在这个边界中，异常样本在边界之外，从而可以检测出异常。Distribution map方法同样是使用正常样本进行训练，得到正常样本的分布，输入异常样本的时候，分布不同于正常样本。Memory bank方法是将正常样本的特征进行存储，在测试阶段进行计算距离，异常样本通常会距离较远，这个方法重点在如何对数据进行降维的同时不损失性能。
- 基于重建的方法通常在图像分类弱于基于嵌入的方法，但是像素优于基于嵌入的方法，同样是无监督，后面延伸出很多异常合成方法，在特征层面加高斯噪声即全局异常，在局部移植异常纹理即局部异常等等，对于正常样本叠加异常再进行复原，。
- 异常合成通常是扩散模型做的多，不过有两种，一种是只做生成式任务（有监督），另一种是生成式任务（有监督）+重建任务。只做生成式任务的只是对于生成的数据进行检测，对比的时候使用其他生成模型的数据与自己生成的数据进行比较，比较方法是使用相同的U-Net框架，哪一个分数较高。生成式任务+重建任务同行是生成式任务作为异常生成的一个模块，不过在测试阶段不需要这个模块。

#### 基于CLIP的零样本异常检测

- 《WinCLIP: Zero-/Few-Shot Anomaly Classification and Segmentation》
- 《A Zero-/Few-Shot Anomaly Classification and Segmentation Method for CVPR 2023 VANDWorkshop Challenge Tracks 1&2: 1st Place on Zero-shot AD and 4th Place on Few-shot AD》
- 《Random Word Data Augmentation with CLIP for Zero-Shot Anomaly Detection》
- 《CLIP-AD:A Language-Guided Staged Dual-Path Model for Zero-shot Anomaly Detection》
- 《ANOMALYCLIP: OBJECT-AGNOSTIC PROMPT LEARNING FOR ZERO-SHOT ANOMALY DETECTION》
- 《Bootstrap Fine-Grained Vision-Language Alignment for Unified Zero-Shot Anomaly Localization》
- VCP-CLIP: A visual context prompting model for zero-shot anomaly segmentation》
- 《MUSC: ZERO-SHOT INDUSTRIAL ANOMALY CLASSI-FICATION AND SEGMENTATION WITH MUTUAL SCOR-ING OF THE UNLABELED IMAGES》
- 《AdaCLIP: Adapting CLIP with Hybrid Learnable Prompts for Zero-Shot Anomaly Detection》
- 《FiLo: Zero-Shot Anomaly Detection by Fine-Grained Description and High-Quality Localization》
- 《Do LLMs Understand Visual Anomalies? Uncovering LLM's Capabilities in Zero-shot Anomaly Detection》
- 《GLOCALCLIP: OBJECT-AGNOSTIC GLOBAL-LOCAL PROMPT LEARNING FOR ZERO-SHOT ANOMALY DE-TECTION》

WinClip:

- 之前没有使用Clip进行缺陷检测的模型
- 文本使用对象+状态描述，将正常/异常文本送入文本编码器，取各自特征平均值，得到描绘正常/异常图像的文本向量，代码中向量维度为[1,640].
- 图像上，将图像分为多个尺度进行异常检测，并将多尺度进行融合。图像尺寸为[3,240,240]，vit输入为16*16的图像块，所以一张图片可以分为15*15个图像块，论文中选取2,3两个尺度进行检测，即将15*15的网格分别使用2*2,3*3的mask提取，在进入transformer之前特征为[N,225,896],225为网格数量，使用多尺度后，2*2的尺度有196个，3*3的尺度有169个。即[N,225,896]->[196,N.4,896]+[169,N.9,896]，将这个区域所有网格计算平均异常分数。最后的向量为[1,640]。

ClipAD:

- WinClip出现了噪点，以及异常区域得分较低，正常区域的分较高。
- 提出了RVS（文本描述的范式），当通过直接平均获得代表向量时，RVS退化为CPE。获取向量和以前一样，只不过是如何从向量中提取适合的向量的方法改变了
- SDP：     分级结构：将 ViT 划分为多个阶段，每个阶段提取不同层次的特征。     双路径处理：在每个阶段使用双路径结构，其中一条路径采用 V-V 注意力，确保异常特征不被正常特征干扰。     多层分割图融合：在每个阶段生成分割图，并将各阶段的分割图融合生成最终的异常检测结果。     SDP+（通过训练添加一个线性层，解决图像与文本未对齐的偏差）     各自添加线性映射层再融合     

RWAD:

- 将CLIP作为训练FNN的数据源
- 由于来自  CLIP 的类别不可知且高度多样化的样本，本方法可以应用于未知对象类别的样本，并在零样本 AD 设置中无需任何提示集成即可实现有竞争力的性能。
- 设计随机文本训练一个二分类FNN，利用文本与异常相似度，将异常输入进FNN，计算异常的分

VAND-APRIL-GAN:

- 如何在没有或只有少量正常参考图像的情况下，快速适应多类别的异常检测问题
- 充分利用了CLIP模型浅层和深层的特征，在零样本和小样本设置中均进行多阶段特征的比对。     多层次特征利用：充分利用了CLIP模型浅层和深层的特征，在零样本和小样本设置中均进行多阶段特征的比对。          多记忆库策略：在小样本设置中，通过存储参考图像的特征于多记忆库中，与测试图像特征进行逐层比较，从而提升检测精度。          轻量级且高效的训练：使用少量数据快速训练，展示了在多个数据集（VisA和MVTec AD）上的高效性能。

AnoClip:

- 领域感知状态提示：设计了一种新的统一领域感知对比状态提示模板，以“一个[领域]的[状态][类别]照片”为模板，通过对比提示提升文本和视觉之间的对齐效果。     
- 局部感知视觉标记提取：通过修改CLIP的注意力机制，引入一种训练自由的值到值（V-V）注意机制，从视觉编码器中提取局部感知的patch标记，以增强细粒度的异常检测能力。     测试时自适应(TTA)：引入了一种轻量级的自适应模块，通过对视觉编码器中的伪标签和噪声扰动标记进行优化，在测试阶段进一步改进异常定位结果。

AnomalyClip:

- 对象无关的文本提示设计：采用学习型提示模板，通过CLIP模型的文本编码器进行学习。正常性和异常性的提示模板被设计为具有泛化能力的语义表示，适用于各类物体而不局限于特定对象。
- 联合全局和局部上下文优化：提出了全局和局部的上下文优化方法，以在全局和细粒度的层面上捕捉异常的语义。具体来说，利用全局损失函数来学习对象无关的全局异常和正常性语义，同时在视觉编码器的中间层使用局部损失函数来捕捉细粒度的异常区域。

VCP-CLIP:

- 将视觉信息融入到文本编码器中，使得不依赖于手工指定的对象类别。     UTP 的核心目标：通过设计统一的文本模板，将文本提示从产品类别独立出来，使得模型在不同的任务和数据集上具有更好的通用性和泛化性。     DTP：在每层文本编码器的输入中，DTP 会插入一组 可学习的嵌入（Learnable  Embeddings），这些嵌入会随着训练动态调整，以优化文本特征。
- VCP-CLIP  使用 CLIP 的图像编码器提取两种不同层次的视觉特征：     全局特征：从 [CLS] token 中获取图像的全局嵌入，用于在 Pre-VCP 模块中优化文本提示。     细粒度特征：从中间多个层（如第 6、12、18、24 层）提取 patch-level 特征，用于在 Post-VCP  模块中实现更精细的异常检测和分割。

MuSc:

- 图像之间patch最高相似并通过image  cls加权

AdaCLIP:

- 对着代码和论文精度了一遍，里面提出了一种结合图像和文本语义的异常检测框架，利用提示机制引导多模态特征学习。在预训练的 CLIP 模型中加入动态和静态提示，整个项目工作量极大，一周一直再对照论文调试代码。

- 文本编码器处理正常和异常描述，例如 "a photo of a normal object" 和 "a photo of a damaged object"。动态/静态提示同样被插入到文本编码器的部分层，增强对异常语义的建模。
- 从预训练的 CLIP 图像编码器中提取多层特征（如第6、12、18、24层）。动态/静态提示被插入图像编码器，优化图像特征的语义表示。
- 动态提示根据图像或文本上下文生成，静态提示为全局可学习参数。提示向量通过与输入特征的交互，引导模型关注异常区域或语义。将图像 Patch 特征进行语义聚类，生成语义嵌入 。图像特征与文本特征通过余弦相似度计算异常分数。

FiLo:

- FiLo 包含两个关键组件：细粒度描述 (FG-Des) 和高质量定位 (HQ-Loc)。 FG-Des 利用大型语言模型 ( LLMs ) 为每个对象类别生成详细的异常描述，从而提高异常检测的准确性和可解释性。 HQ-Loc 通过结合使用 Grounding DINO 的初步定位、位置增强文本提示和多尺度多形状跨模式交互 (MMCI) 模块来改进定位，从而可以对各种尺寸和形状进行精确的异常检测。
- 首先，通过LLM生成每个类别可能存在的细粒度异常类型列表，将这些描述填入可学习的文本模板中，获取表示“正常”和“异常”语义的文本特征。然后，将待检测图像和细粒度异常描述输入Grounding DINO，获得初步的异常定位框，并将位置信息添加到文本特征中。接着，将待检测图像输入CLIP图像编码器提取中间层特征，这些特征通过MMCI模块与包含位置信息的文本特征交互，生成异常分数图。最后，综合各中间层的异常分数图，得到最终的异常图和全局异常得分。

GLOCALCLIP:利用了全局文本和局部文本以及对比学习,

#### 基于CLIP的零样本异常检测心得

实验表明，CLIP 在工业异常检测任务中能够较好地识别多种异常模式，尤其是在无标签或零样本的情况下，其灵活性和适应性非常突出。然而，对于特定工业场景，仍需优化文本描述以最大化模型性能。

异常的定义非常重要，基于语言的异常定义需要专业知识的支持。例如，在制造业中，“裂纹”“划痕”“污染”等异常描述需要精确与实际场景相符，文本定义的准确性直接影响检测效果。

工业场景中某些细微异常（如微小裂痕）可能难以通过通用的视觉-语言对齐捕获。这种情况下，可以通过增强异常描述的细粒度信息或结合高分辨率图像增强模型的检测能力。

目前已经基于AnomaliyCLIP做了一些实验，将文本加上方位、在文本token中加入图像的特征再训练等。均没有对Anomalyclip有特别大的提升，其中加上方位后，pix aupoc分数提高了0.5%。

#### 反思与展望

##### 1、短期工作安排：

将最近实验整理，为发表论文、专利做计划。

##### 2、长期工作安排：

保持阅读文献的习惯，并且按部就班地推进实验与毕业论文撰写。